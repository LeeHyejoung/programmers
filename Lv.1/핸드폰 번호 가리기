class Solution {
    public String solution(String phone_number) {
        String answer = "";
        int length = phone_number.length();
        int i;
        
        for (i = 0; i < length - 4; i++) {
            answer += "*";
        }
        
        answer += phone_number.substring(i, length);
        
        
        return answer;
    }
}
