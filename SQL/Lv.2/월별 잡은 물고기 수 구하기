-- 코드를 작성해주세요
SELECT COUNT(*) FISH_COUNT, CAST(DATE_FORMAT(TIME, '%c') AS UNSIGNED) MONTH
FROM FISH_INFO
GROUP BY CAST(DATE_FORMAT(TIME, "%c") AS UNSIGNED)
ORDER BY CAST(DATE_FORMAT(TIME, "%c") AS UNSIGNED)
