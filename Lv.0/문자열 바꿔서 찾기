def solution(myString, pat):
    str = ""
    for s in myString:
        if s == "A":
            str += "B"
        else:
            str += "A"
                    
    if (pat in str):
        return 1
    else:
        return 0
