class Solution {
    public String solution(String my_string, String alp) {
        String answer = "";
        String str = alp.toUpperCase();
        answer = my_string.replace(alp, str);
        
        return answer;
    }
}
