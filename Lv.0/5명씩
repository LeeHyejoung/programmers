class Solution {
    public String[] solution(String[] names) {
        double length = Math.ceil((double)names.length / 5);
        String[] answer = new String[(int)length];
        
        for (int i = 0; i < length; i++) {
            answer[i] = names[i * 5];
        }
        
        return answer;
    }
}
