def solution(arr):
    if (arr.count(2) == 0):
        return [-1]

    idx = 0
    for i in range(len(arr)):
        if arr[i] == 2:
            idx = i
    answer = arr[arr.index(2):idx + 1]
    return answer
