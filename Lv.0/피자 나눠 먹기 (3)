
class Solution {
    public int solution(int slice, int n) {
        int answer = 0;
        double pizza = (double)n / slice;
        
        answer = (int)(Math.ceil(pizza));
        
        return answer;
    }
}
