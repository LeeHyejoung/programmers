def solution(binomial):
    answer = 0
    arr = binomial.split(" ")
    op = arr[1]
    arr[0], arr[2] = int(arr[0]), int(arr[2])
    if op == "+":
        answer = arr[0] + arr[2]
    elif op == "-":
        answer = arr[0] - arr[2]
    else:
        answer = arr[0] * arr[2]
    return answer
